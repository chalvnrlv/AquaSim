import { Fn } from 'three/src/nodes/tsl/TSLBase.js';
import { screenUV } from 'three/src/nodes/display/ScreenNode.js';
import { viewportDepthTexture } from 'three/src/nodes/display/ViewportDepthTextureNode.js';
import { linearDepth } from 'three/src/nodes/display/ViewportDepthNode.js';

export const viewportSafeUV = /*@__PURE__*/ Fn( ( [ uv = null ] ) => {

	const depth = linearDepth();
	const depthDiff = linearDepth( viewportDepthTexture( uv ) ).sub( depth );
	const finalUV = depthDiff.lessThan( 0 ).select( screenUV, uv );

	return finalUV;

} );
